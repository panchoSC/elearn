// Este archivo es el punto de entrada de la aplicación.
// Se puede utilizar para importar otros archivos JS y CSS.
// La idea es que este archivo sea el único que se importe en el HTML.
// Se ejecuta cuando el DOM está completamente cargado.

document.addEventListener("DOMContentLoaded", () => {
    console.log("Elearn está cargado y listo.");
});
